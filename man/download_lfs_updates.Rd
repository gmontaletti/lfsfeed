% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_updates.R
\name{download_lfs_updates}
\alias{download_lfs_updates}
\title{Download all newly-updated Labour Force Survey bulk files.}
\usage{
download_lfs_updates(
  dest_dir = NULL,
  state_path = NULL,
  url = NULL,
  categories = default_categories(),
  force = FALSE,
  retry_max = 3L,
  timeout_s = 300L,
  quiet = FALSE
)
}
\arguments{
\item{dest_dir}{Directory in which the \code{.tsv.gz} files are written.
Defaults to \code{tools::R_user_dir("lfsfeed", "data")}.}

\item{state_path}{Path of the JSON state file. Defaults to
\code{\link[=lfs_state_path]{lfs_state_path()}}.}

\item{url}{RSS feed URL. \code{NULL} = the Eurostat default.}

\item{categories}{Character vector of \verb{<category>} values to keep.
Defaults to \code{c("UPDATED_DATASET_DATA", "UPDATED_DATASET_STRUCTURE_DATA", "NEW_DATASET")}. Set to \code{NULL} to keep every category.}

\item{force}{If \code{TRUE}, ignore the state file and (re)download every
matching item.}

\item{retry_max}{Maximum number of HTTP attempts per file.}

\item{timeout_s}{Per-request timeout in seconds.}

\item{quiet}{If \code{TRUE}, suppress informational messages.}
}
\value{
A tibble with columns \code{code}, \code{pub_date}, \code{description},
\code{category}, \code{path}, \code{bytes}, \code{status}, \code{message} — one row per
dataset considered.
}
\description{
Polls the Eurostat statistics-update RSS feed, restricts to LFS
datasets, diffs the resulting items against a small JSON state file,
and downloads the bulk \code{.tsv.gz} for every item whose pubDate is
strictly newer than the last-seen value (or all of them, if
\code{force = TRUE}). The state file is updated only for downloads that
succeed.
}
\details{
Errors during a single download are isolated: the orchestrator
continues with the remaining items and the manifest reflects the
failure as \code{status = "failed"}.

Do not run two instances against the same \code{state_path} concurrently;
the state file uses last-write-wins semantics.
}
\examples{
\dontrun{
tmp <- tempfile("lfsfeed-")
dir.create(tmp)
m <- download_lfs_updates(
  dest_dir   = tmp,
  state_path = file.path(tmp, "state.json")
)
print(m)
}
}
