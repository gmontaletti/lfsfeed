% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_feed.R
\name{fetch_lfs_feed}
\alias{fetch_lfs_feed}
\title{Fetch the Eurostat statistics-update RSS feed.}
\usage{
fetch_lfs_feed(
  url = default_feed_url(),
  filter = TRUE,
  categories = default_categories(),
  timeout_s = 30L,
  user_agent = default_user_agent()
)
}
\arguments{
\item{url}{URL of the RSS feed. Defaults to the Eurostat dissemination
feed at
\verb{https://ec.europa.eu/eurostat/api/dissemination/catalogue/rss/en/statistics-update.rss}.}

\item{filter}{Logical. If \code{TRUE} (default), restrict to items whose
dataset code begins with \code{lfs} (case-insensitive).}

\item{categories}{Character vector of \verb{<category>} values to keep.
Defaults to \code{c("UPDATED_DATASET_DATA", "UPDATED_DATASET_STRUCTURE_DATA", "NEW_DATASET")}. Set to \code{NULL} to keep every category.}

\item{timeout_s}{Per-request timeout in seconds.}

\item{user_agent}{User-Agent header sent with the request.}
}
\value{
A tibble with columns \code{code}, \code{title}, \code{description},
\code{pub_date} (POSIXct UTC), \code{link}, \code{category}.
}
\description{
Polls the Eurostat dissemination \code{statistics-update.rss} feed and
returns one row per \verb{<item>} as a tibble. By default, restricts to
Labour Force Survey (LFS) datasets and to the \verb{<category>} values
that affect the bulk file.
}
\examples{
\dontrun{
feed <- fetch_lfs_feed()
head(feed)
}
}
