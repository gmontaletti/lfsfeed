% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_public.R
\name{get_lfs_metadata}
\alias{get_lfs_metadata}
\title{Get rich metadata for a Labour Force Survey dataset.}
\usage{
get_lfs_metadata(
  code,
  dest_dir = NULL,
  lang = c("en", "fr", "de", "it"),
  refresh = FALSE,
  quiet = FALSE,
  retry_max = 3L,
  timeout_s = 60L
)
}
\arguments{
\item{code}{character(1). Eurostat dataset code (case-insensitive).}

\item{dest_dir}{Directory where the RDS cache lives. Defaults to
\code{tools::R_user_dir("lfsfeed", "data")}.}

\item{lang}{Character vector of languages to keep in the codelist
tibbles. Allowed: \code{"en"}, \code{"fr"}, \code{"de"}, \code{"it"}. The Italian
column is always retained (see above); other unrequested columns
are dropped from the codelists.}

\item{refresh}{If \code{TRUE}, ignore the on-disk cache and re-fetch.}

\item{quiet}{If \code{TRUE}, suppress informational messages.}

\item{retry_max, timeout_s}{Forwarded to the underlying \code{httr2} call.}
}
\value{
A list with elements \code{title} (named chr), \code{description},
\code{dimensions} (tibble), \code{codelists} (named list of tibbles with
columns \code{code}, \code{label_en}/\code{label_fr}/\code{label_de} per \code{lang}, and
\code{label_it}). Carries attributes \code{schema_version}, \code{code},
\code{fetched_at}, \code{source_url}, \code{italian_status},
\code{italian_matched_codelists}.
}
\description{
Fetches the dataflow + DSD + every referenced codelist from Eurostat
in one HTTP call (\code{?references=descendants}), parses the SDMX 2.1
XML, and \emph{when the optional} \pkg{istatlab} \emph{package is
installed} augments each codelist with a best-effort \code{label_it}
column sourced from ISTAT (Italy's national statistics institute).
The result is cached as RDS at \verb{<dest_dir>/metadata/<code>.rds} and
returned to the caller.
}
\details{
Italian coverage is intentionally partial: only Eurostat codelists
with an entry in the bundled mapping
(\code{inst/extdata/eurostat-istat-codelists.csv}) are attempted, and only
codes present in the matched ISTAT codelist receive a non-fallback
label. Codes with no ISTAT match get \code{label_it = label_en}. The
returned object always has a \code{label_it} column, regardless of
coverage, so downstream joins have a stable schema. The
\code{"italian_status"} attribute documents which path was taken
(\code{"ok"}, \code{"skipped: istatlab not installed"}, \code{"istat-error: ..."},
\code{"skipped: lang"}).
}
\examples{
\dontrun{
m <- get_lfs_metadata("lfsi_jhh_a")
m$title           # named chr by language
m$dimensions      # tibble of dimensions
m$codelists$geo   # tibble with code + label_* columns
attr(m, "italian_status")
}
}
