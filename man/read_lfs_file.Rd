% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_lfs_file.R
\name{read_lfs_file}
\alias{read_lfs_file}
\title{Read a downloaded Labour Force Survey \code{.tsv.gz} into a tibble.}
\usage{
read_lfs_file(path, ...)
}
\arguments{
\item{path}{Path to a \code{.tsv.gz} file (typically produced by
\code{\link[=download_lfs_updates]{download_lfs_updates()}}).}

\item{...}{Further arguments passed to \code{\link[utils:read.table]{utils::read.delim()}}.}
}
\value{
A tibble.
}
\description{
Convenience wrapper around \code{\link[utils:read.table]{utils::read.delim()}} for files written by
\code{\link[=download_lfs_updates]{download_lfs_updates()}}. Returns a tibble of strings; no SDMX-style
column splitting is performed. For richer parsing, install the
\pkg{eurostat} package and call \code{eurostat::get_eurostat()} with the
dataset code.
}
\details{
Eurostat encodes missing values as \code{:} and uses the first column to
carry several comma-separated dimension keys (e.g. \verb{freq,sex,age,unit\\time}).
This reader treats \code{:} as \code{NA} but does not split that combined column.
}
\examples{
\dontrun{
read_lfs_file("estat_lfsa_egan.tsv.gz")
}
}
